<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Camp22Quiz" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_sapphire</item>
        <item name="colorPrimaryVariant">@color/veridian_green</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/app_text_size</item>
        <item name="materialButtonStyle">@style/ButtonStyle</item>
        <item name="fontFamily">sans-serif</item>
        <item name="android:textColorHint">@color/somewhat_transparent_white</item>
    </style>

    <!--    background of pop up menu-->
    <style name="ThemeOverlay.MyTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:backgroundTint">@color/veridian_green</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="SearchViewTheme" parent="Widget.AppCompat.SearchView">
        <!--        query hint color-->
        <item name="android:textColorHint">@color/white</item>
        <item name="android:textSize">15sp</item>
        <!--        close button color-->
        <item name="android:tint">@color/light_grey</item>
        <!--        query text color-->
        <item name="android:editTextColor">@color/white</item>
    </style>

</resources>